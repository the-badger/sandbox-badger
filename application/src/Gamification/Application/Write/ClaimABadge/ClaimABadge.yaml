Badger\Gamification\Application\Write\ClaimABadge\ClaimABadge:
  properties:
    badgeId:
      - Uuid: { message: 'The attribute badgeId should be valid' }
      - NotBlank: { message: 'The attribute badgeId cannot be empty' }
    memberId:
      - Uuid: { message: 'The attribute memberId should be valid' }
      - NotBlank: { message: 'The attribute memberId cannot be empty' }
